edit /verify-student such that it will only take email while verifying and then enable the claim button

/verify/verify route already gives us the image URL, make the NFT for that

make a backend route which will chnage the clamied attribute to true in redis after beng clamied( also post the wallet address in it )

after this edit the /verify-student again like if the NFT has already claimed then show NFT already clamied in xxxx wallet.